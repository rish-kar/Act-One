spring:
  datasource:
    url: jdbc:h2:mem:tickets-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: never
  mail:
    host: localhost
    port: 2525
    username: test
    password: test
    properties:
      mail:
        smtp:
          allowutf8: true
        mime:
          charset: UTF-8
logging:
  level:
    com.prarambh.act.one: INFO
actone:
  admin:
    purge-password: ${ACTONE_ADMIN_PASSWORD:prarambh-admin-delhi}
  email:
    enabled: false
    bcc: ""
  ticket-card:
    enabled: true
    templateResource: "classpath:/static/Card.png"
